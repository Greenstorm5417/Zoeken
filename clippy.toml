# Clippy configuration for the whole workspace.
# Lint levels live in `[workspace.lints]` in the root Cargo.toml; this file only
# tunes the configurable thresholds for individual lints.
msrv = "1.85"
