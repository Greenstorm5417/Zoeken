# Example env for docker compose. Copy to .env and fill in values.
#
#   cp .env.example .env
#   # then set APP_SECRET_KEY (compose refuses to start without it)

# Required. Generate with: openssl rand -hex 24
APP_SECRET_KEY=

APP_PUBLIC_INSTANCE=true
ZOEKEN_PORT=8888
